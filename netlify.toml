# Netlify builds the site from the source: JSX precompiled, React/Firebase/Tailwind self-hosted.
# If the build fails, Netlify keeps serving the previous deploy.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
